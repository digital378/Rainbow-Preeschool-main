/**
 * Re-export from the shared verified-rating constant so client pages continue
 * to import from this path without change.
 *
 * The authoritative source of truth lives in shared/verified-rating.ts —
 * edit that file when refreshing figures from Google Business Profile.
 */
export { VERIFIED_RATING } from "@shared/verified-rating";
