ROLE AND CONTEXT

You are performing a staged SEO remediation on this codebase, driven by an external code-verified audit conducted on 25 July 2026. This session is PHASE 1 ONLY: investigate and report. You will not modify, create, or delete a single file in this phase. A separate prompt will authorise implementation.

Background you need: this app serves pre-rendered HTML to crawlers via server/bot-ssr.ts (matching ~30 user-agents) while human visitors receive the React SPA. Page SEO data lives in server/ssr-pages.ts. Today, three production issues were already fixed — a Cloudflare "Cache Everything" page rule that was poisoning bot/human HTML, Cloudflare's Managed robots.txt blocking AI crawlers, and self-serving aggregateRating plus self-authored Review schema in SSR. Do not revisit or undo those.

═══════════════════════════════════════
ABSOLUTE CONSTRAINT — READ TWICE
═══════════════════════════════════════

You must NEVER invent, estimate, infer, adjust, round, or "improve" any factual claim about this school. That includes enrolment totals, awards, employee counts, child-to-teacher ratios, batch sizes, opening hours, class timings, staff qualifications, staff gender composition, facilities, certifications, and review ratings.

The audit found that several such claims currently in this repo cannot be corroborated from any public source, and that at least two of them contradict each other. There is a real possibility these were generated by an AI agent in a previous session rather than supplied by the school. Your task is to LOCATE and REPORT them with complete precision — never to rewrite them, and never to make them "more plausible". If a correct fix would require knowing a real-world fact about Rainbow Preschool, that fact request goes in your report for a human to answer.

Producing a confident-sounding but unverified number in this phase would actively harm the business. Reporting "I found this claim in 4 files and cannot verify it" is the correct and valuable output.

═══════════════════════════════════════
DELIVERABLE
═══════════════════════════════════════

Create nothing on disk. Output your findings directly in chat, structured under headings A–F below. Use exact file paths and line numbers throughout. Where you quote existing text, quote it verbatim.

─────────────────────────
A. CLAIMS REGISTER
─────────────────────────

Search the ENTIRE repository — server/, client/, shared/, public/, client/public/, docs/, and any .md files at root — for each claim category below. Search JSON-LD schema objects, llms.txt, llms-full.txt, FAQ content arrays, SSR contentSections, React JSX copy, and image alt text.

Suggested search patterns (use these as a starting point, then broaden):
  1. Enrolment totals:      /1,00,000|1,00000|100000|100,000|lakh|children served|students served/i
  2. Awards:                /award|India Today|ScooNews|Economic Times|recognition|winner/i
  3. Employee counts:       /numberOfEmployees|staff strength|employees|team of/i
  4. Hours and timings:     /openingHours|08:00|8:00 AM|18:00|6:00 PM|8:30|11:30|12:30|3:30|15:30|Monday to|Mon-Sat|Mon-Fri/i
  5. Batch sizes / ratios:  /batch|10-12|10–12|12-15|12–15|children per|ratio|class size/i
  6. Staff composition:     /female|women|100% trained|all staff/i
  7. Safety and curriculum: /CCTV|surveillance|NEP|NEP 2020|ECE|ECCE|qualified|certified/i
  8. Rating:                /4\.9|487|VERIFIED_RATING|aggregateRating|ratingValue|reviewCount|parent reviews/
  9. History:               /2007|18 years|19 years|since 2007|founded|established/i
 10. Centre count:          /6 centres|six centres|6 centers|our centres/i

Present the results as a table with these exact columns:

| # | Claim (verbatim) | File | Line | Emitted to | Category | Duplicated in |

Where "Emitted to" is one of: GOOGLE (via bot-SSR HTML/JSON-LD), AI-ENGINES (via llms.txt or llms-full.txt), PARENTS (visible React UI), or MULTIPLE — and "Duplicated in" lists every other file carrying the same claim, so I can see the blast radius of correcting each one.

Then add a short subsection: "CONTRADICTIONS FOUND" — list any two claims in the repo that state different values for the same fact. The audit already identified one: llms.txt states hours of Monday–Saturday 08:00–18:00, while the FAQ schema on /playgroup states Monday–Friday batches of 8:30–11:30 and 12:30–15:30. Confirm this and find any others (e.g. differing centre counts, differing age ranges, differing ratios between llms.txt, llms-full.txt, ssr-pages.ts and React pages).

Finally add: "FACTS I NEED FROM A HUMAN" — a numbered list of the specific questions someone at the school must answer before any of these claims can be corrected. Be precise ("How many children have enrolled across all centres since 2007?"), not vague ("verify enrolment").

─────────────────────────
B. LOCALITY PAGE OVERLAP
─────────────────────────

The repo contains two parallel families of locality pages, both listed in SITEMAP_ENTRIES in shared/sitemap-entries.ts:
  Family 1: /preschool-in-manpada-thane, /preschool-in-hariniwas-thane, /preschool-in-anand-nagar-thane, /preschool-in-dhokali-thane, /preschool-in-kalwa-thane, /preschool-in-kasarvadavali-thane
  Family 2: /playgroup-in-manpada, /playgroup-in-kalwa, /playgroup-in-anand-nagar, /playgroup-in-kasarvadavali, /playgroup-in-dhokali, /playgroup-near-ghodbunder-road

Meanwhile docs/seo-topic-url-map.md states: "Each locality has exactly one page. Do not duplicate per programme."

Report:
  1. For each of the 12 pages: its route definition, which component renders it, its ssr-pages.ts entry title / description / h1 / canonical, and approximate unique word count of its SSR contentSections.
  2. Which localities appear in BOTH families.
  3. A concrete assessment of content overlap between each matched pair — quote the opening sentences of each so I can judge how similar they are.
  4. Whether any canonical tags already point one family at the other.

Then propose two options WITHOUT implementing either:
  OPTION A (consolidate): 301 each /playgroup-in-{area} to its /preschool-in-{area}-thane counterpart via server/redirects.ts, remove the redirected URLs from SITEMAP_ENTRIES, and strengthen each locality page with a playgroup/nursery/kindergarten section. List every file that would change and the exact redirect pairs, including what to do with /playgroup-near-ghodbunder-road which has no /preschool-in-ghodbunder counterpart.
  OPTION B (differentiate): keep both, give the playgroup pages genuinely age-specific H1s, FAQs and content. List what unique content each page would need and flag that this requires new copy — which means new factual claims, which returns to the constraint above.

State which option you recommend and why, in terms of link-equity concentration and maintenance cost.

─────────────────────────
C. CATCHMENT PAGES
─────────────────────────

For /play-school-near-majiwada, /play-school-near-naupada and /play-school-near-ghodbunder-road:
  1. Confirm whether any actual Rainbow centre exists in that locality. Cross-reference shared/centre-data.ts and the LocalBusiness schema in server/ssr-pages.ts — the six centres with real addresses appear to be Manpada, Hariniwas, Anand Nagar, Dhokali, Kalwa and Kasarvadavali.
  2. Quote how each page describes Rainbow's presence in that area. Does it name the actual serving centre and its distance, or does it imply a centre is located there?
  3. Report each page's file size and approximate unique word count, compared against a true locality page such as /preschool-in-manpada-thane.
  4. Report whether Organization areaServed in ssr-pages.ts lists localities where no centre exists (the audit noted Naupada, Majiwada and Kolshet Road).

Do not rewrite these pages. Propose the shape of an honest fix (naming the serving centre and travel distance) and identify which facts you would need from a human to write it.

─────────────────────────
D. SOURCE-OF-TRUTH DRIFT
─────────────────────────

  1. docs/seo-topic-url-map.md lists /preschool-in-brahmand-thane and /preschool-in-vasant-vihar-thane as locality canonicals. Determine whether these exist as routes, as ssr-pages.ts entries, and in SITEMAP_ENTRIES. Report the exact state of each — do they 404, redirect, render, or not exist at all?
  2. shared/seo-config.ts contains REDIRECT_MAP with a comment stating it is "currently dead code". Search the whole repo for imports of REDIRECT_MAP and getRedirectTarget. Confirm whether anything at runtime consumes them, and whether its entries agree or disagree with the live map in server/redirects.ts. List any disagreements.
  3. shared/seo-config.ts also exports CORE_PAGES, PROGRAMME_PAGES, HIGH_INTENT_PAGES, LOCAL_PRESCHOOL_PAGES and LOCAL_PLAYGROUP_PAGES, which appear to duplicate SITEMAP_ENTRIES in shared/sitemap-entries.ts. Report whether anything imports them and whether the two lists agree — note that HIGH_INTENT_PAGES uses priorities 0.98/0.95/0.92 while sitemap-entries.ts uses 1.0 for the same URLs.
  4. docs/seo-topic-url-map.md references client/src/pages/preschool-readiness-quiz.tsx; the actual file appears to be readiness-quiz.tsx. Confirm, and check the sitemap URL /preschool-readiness-quiz actually routes to it.
  5. Report on RPI_Pages_Inventory.xlsx and rainbowpreschools-url-audit.xlsx in the repo root — are they referenced by any code or documentation, and do they represent yet another parallel page inventory?
  6. Verify that every URL in SITEMAP_ENTRIES has a corresponding entry in ssr-pages.ts getPageSEO. List any sitemap URL that would fall through to the bot-SSR 404 handler — that would be a live SEO defect.

─────────────────────────
E. PHONE NUMBERS AND NAP
─────────────────────────

List every phone number appearing anywhere in the repo, with file, line, format, and context (schema telephone field vs visible UI text). The audit observed at least three formats: "022-47762019", "91365 78589", "+91-8291568972". Report which are centre-specific and which is the central admissions number, and flag any number that appears in schema in a non-E.164 format.

─────────────────────────
F. PROPOSED PHASE 2 CHANGE LIST
─────────────────────────

Conclude with a table of every change you would make in Phase 2, split into two groups:
  SAFE — deterministic changes requiring no external facts.
  BLOCKED — changes that cannot proceed until a human answers a question from section A.

For each SAFE row give: file(s), precise change, risk level, and how you would verify it.

Then STOP. Make no changes. Wait for my approval.