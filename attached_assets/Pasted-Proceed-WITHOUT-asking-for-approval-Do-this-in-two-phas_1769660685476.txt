Proceed WITHOUT asking for approval. Do this in two phases in the SAME response:
PHASE 1 (Dry-run outputs) and PHASE 2 (Implementation: code changes + files edited).
Do not pause for confirmations.

You already discovered:
- React + TS + Vite SPA
- Wouter routing in client/src/App.tsx
- Redirects in server/redirects.ts
- SEO in client/src/components/seo.tsx
- Sitemap at client/public/sitemap.xml (currently incomplete)
- robots.txt at client/public/robots.txt (currently wrong domain)
- canonical uses www (correct)

Now execute the full fix safely.

========================================================
PHASE 1 — DRY-RUN OUTPUTS (MUST PRODUCE TABLES)
========================================================

1) Domain Canonicalisation Decision (MUST STATE):
- Preferred domain: https://www.rainbowpreschools.com
- Rules: http→https, non-www→www, trailing slash consistency (no trailing slash unless existing routes require it)
- Confirm how redirect handling works in Express and where to implement it.

2) Sitemap inventory:
- Read all Wouter routes from client/src/App.tsx and list all indexable routes.
- Include:
  - core pages: /, /about, /programmes, /contact, /blog
  - programme pages: /playgroup, /nursery, /kindergarten, /kids-activity-club, /summer-camp, /happy-times
  - local pages: /preschool-in-... and /playgroup-...
  - all blog posts (the 100+ list is assumed present as routes)
- Output: total counts by type (core/programme/local/blog).

3) Duplicate clusters (Blog canonical plan):
Create a redirect/consolidation plan for the clearest clusters that already exist in the URL list:
- preschool vs daycare duplicates:
  /preschool-vs-daycare-difference
  /preschool-vs-daycare-difference-explained
  /preschool-vs-daycare-understanding-difference
  -> select ONE canonical target and redirect the other two
- preschool admission process duplicates:
  /preschool-admission-process-explained
  /preschool-admission-process-guide
  /preschool-admission-process-explained (if repeated)
  -> select ONE canonical target and redirect the others
- any other exact/near duplicate slugs (detect algorithmically by similarity)
Output: Redirect Map table with columns:
OLD URL | NEW URL | TYPE (301) | REASON

4) Noindex plan:
Create a noindex list for low-intent blog topics:
- festival activities (holi/diwali/rainy-season/sports-day)
- generic kids games/quotes/GK content
- “national-symbols-of-india-for-kids” and similar
Output: Noindex table:
URL | robots tag | reason | keep in sitemap? (NO)

5) Missing high-intent landing pages:
Create these routes/pages (index,follow):
- /preschool-near-me
- /best-preschool-in-thane
- /preschool-admissions (or /admissions if preferred; choose ONE and stick with it)
Output: table with:
URL | Primary keyword | Internal links it must receive (from homepage + programme + relevant blogs)

6) Internal linking rules:
Define an internal linking module that auto-inserts at end of every blog post:
- links to /preschool-admissions, /preschool-near-me, /best-preschool-in-thane, /playgroup, /nursery, /kindergarten
- PLUS context-based local link when the slug contains “thane”, “kalwa”, “manpada”, “dhokali”, “kasarvadavali”, “anand-nagar”, “hariniwas”
Output: rules and mapping table.

========================================================
PHASE 2 — IMPLEMENTATION (MAKE THE ACTUAL CHANGES)
========================================================

A) Fix robots.txt domain inconsistency:
- Update client/public/robots.txt to reference the www domain and sitemap URL:
  Sitemap: https://www.rainbowpreschools.com/sitemap.xml
- Ensure it does not block important pages.

B) Implement domain redirects and trailing slash policy in Express:
- In server entry (or middleware), enforce:
  - http→https
  - non-www→www
  - consistent trailing slash handling (pick one)
- Ensure querystrings are preserved.

C) Build an automated sitemap generator (do NOT hand-maintain XML):
- Create a script that outputs client/public/sitemap.xml during build or dev:
  - Input sources:
    1) a route registry file you create (exported array)
    2) blog slug list (if stored)
    3) local pages list (static array if needed)
- Exclude:
  - noindex pages
  - redirected pages
  - /ad and /ad-google
- Ensure lastmod is current date, but do NOT fake freshness per URL—use build date.
- After generation, print summary: number of URLs in sitemap by type.

D) Implement redirects in server/redirects.ts:
- Add all 301 redirect mappings from PHASE 1.
- Ensure redirects preserve query strings.
- Add unit-like validation or logging to ensure no redirect loops.

E) Implement noindex rules in seo.tsx:
- Add a centralized config list (array) of noindex slugs.
- If current route matches, output:
  <meta name="robots" content="noindex,follow" />
- Ensure canonical still renders.

F) Create the 3 new landing pages:
- Add routes in client/src/App.tsx
- Add new page components under client/src/pages/
- Each must include:
  - one H1
  - Title + meta description
  - FAQ section (visible) + FAQ schema via seo.tsx if present
  - strong CTA
  - internal links to local pages and programme pages

G) Homepage fix:
- Update homepage SEO:
  Title: "Rainbow Preschool International | Early Learning & Admissions"
  Description: brand + trust + admissions, no keyword stuffing
- Ensure only one H1
- Add crawlable internal links section near top:
  - Best Preschool in Thane → /best-preschool-in-thane
  - Preschool Near Me → /preschool-near-me
  - Preschool Admissions → /preschool-admissions (or /admissions; must match chosen)
  - Playgroup → /playgroup, Nursery → /nursery, Kindergarten → /kindergarten

H) Blog internal linking module:
- Implement a reusable component and inject into blog template.
- Must not rely on JS-only interactions; links must be in HTML output.

I) QA Checklist (must execute, not just describe):
- Print results of:
  - sitemap URL count
  - list of redirected URLs tested (at least 10)
  - confirm robots.txt content
  - confirm canonical uses www on a sample page
  - confirm noindex applies to one sample slug
  - confirm new pages are reachable and included in sitemap

OUTPUT REQUIREMENTS:
- Provide a list of every file edited with a short description.
- Provide the final redirect table and noindex list again after implementation.
- Do NOT ask for approval. Proceed and complete.