Add an “Awarded By:” logos section directly below the hero section on the homepage, visually matching the provided reference screenshots:Centered title “Awarded By:”A horizontal row of award/media logosClean spacing, subtle separators (thin vertical lines) between logos (optional but matches reference)PlacementInsert the section immediately after the hero section and before the next homepage content block.Ensure it aligns with the site container/grid width used elsewhere.Functional Requirements1) Light & Dark Mode SupportSection background, text, and logo treatment must adapt to Light and Dark themes.Use theme tokens / CSS variables already present (preferred), e.g.:--bg, --text, --muted, --card, --border, etc.Title text should remain high contrast in both modes.Logos should look clean in both themes:If logos are colored PNG/SVG, keep as-is.If logos are single-color SVGs, allow tinting via CSS (optional).2) Responsive Layout (Mobile → Desktop)Mobile: show the logos in a single row with Right-to-Left continuous scrolling (marquee-like).Tablet / Laptop / Desktop: no marquee. Show as a centered static row/grid with hover zoom-in and soft shadows per logo card.Breakpoints (suggested, adjust to your design system):Mobile: < 768px → scrolling RTLLarge screens: >= 768px → static layout + hover effects3) Mobile Right-to-Left Scrolling BehaviorImplement a smooth, continuous loop (no jump).Logos should scroll from right to left.Pause animation on touch/hover is optional but nice.Must respect accessibility:If user has prefers-reduced-motion: reduce, disable marquee and show static wrapped layout.Implementation suggestion:Duplicate logo list (two tracks) for seamless looping.Use CSS keyframes translateX from 0 to -50% (depending on track width).4) Desktop/Tablet Hover Effects (No Scrolling)Each logo should be in its own “card” or “pill” container:subtle bordersoft shadowslight background tint depending on themeOn hover:scale up slightly (e.g. transform: scale(1.06))increase shadow depthKeep spacing consistent and aligned.Logos should not look “stuck together”—each one feels separate like the reference.UI SpecificationsSection StructureTitle: “Awarded By:”Logos row below titleOptional: thin vertical separators between items (especially in desktop view)Spacing & SizingSection padding: ~40–60px vertical on desktop, ~24–36px on mobileLogo height: ~40–56px desktop, ~28–44px mobile (auto width)Keep consistent alignment and baseline.AssetsUse these example logo keys (replace with actual assets/URLs already in project):India Today(School crest / icon)ScooNewsWorld Education Summit MumbaiETNSA 2023 badgeProvide an easy config array/object so future logos can be added without rewriting layout.Technical RequirementsIf React/Next.jsCreate a reusable component, e.g.:components/AwardedBy.tsxUse it in pages/index.tsx or app/page.tsx right after Hero.Component props:logos: { name: string; src: string; alt: string; href?: string }[]Behavior:Determine marquee vs static using CSS media queries (preferred) or JS useMediaQuery.Ensure images have alt text.Use loading="lazy" where appropriate.If plain HTML/CSS/JSAdd a section block in homepage templateCreate CSS for:.awardedBy.awardedBy__track.awardedBy__itemmarquee animation for mobile onlyPerformanceOptimize logo images:Prefer SVG where possibleOtherwise compressed PNG/WebPAvoid layout shift: set width/height or use consistent logo container height.AccessibilityProper heading level (e.g. <h2>Awarded By:</h2>)alt for each logoDisable animation for prefers-reduced-motionAcceptance Criteria (Checklist)Section appears below hero exactly as requestedLight & dark mode look correct (no washed-out logos / low contrast text)Mobile: RTL continuous scrolling logos (smooth loop)Tablet/Desktop: static row/grid, no scrolling, each logo has hover zoom + shadowWorks on common screen sizes (mobile, tablet, laptop, desktop)prefers-reduced-motion disables marquee and shows static layoutOptional Enhancements (Recommended UX/UI)Subtle gradient edge mask on mobile marquee (fade on left/right edges) to look premium.Consistent logo treatment: If some logos are too tall/wide, apply object-fit: contain and standard container size so the row looks balanced.Auto-wrap fallback for very small screens or reduced motion.DeliverablesNew component/section addedCSS (theme-aware + responsive)Logo list configConfirmed behavior on mobile + desktopShort notes in code comments for adjusting speed, breakpoints, logo sizes